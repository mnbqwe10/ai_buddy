# Domain Docs

This is a single-context repo.

Before exploring, read root `CONTEXT.md` if it exists. Read relevant ADRs under `docs/adr/` if they exist.

If these files do not exist, proceed silently. Do not suggest creating them upfront; producer skills create them lazily when needed.

Use domain vocabulary from `CONTEXT.md` in issue titles, refactor proposals, hypotheses, and test names. If output contradicts an ADR, flag the conflict explicitly.
